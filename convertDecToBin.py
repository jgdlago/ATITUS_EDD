print('Número: ')
numDec = input()

def dec2bin(numDec):
    b = ''
    while numDec != 0:
        if n == 0:
            return ''
        else:
            d2b(n//2) + str(n%2)
            divmod(numDec)
    return print(b[::-1])

    def push(self, numDec): # metodo de inserir item
        if not self.is_empty():
            self._vet.append(numDec)

dec2bin(numDec)
push()